Fix scripts on application page [ Changing payment types breaks things ] @FIXED

Awais to actually do the login page [DESIGNS]
    - LOGIN ENCRYPTION
    - Page descriptions

Tables to center (pls) @FIXED
Fix table headings and values (what shows) @I THINK FIXED
Fix completed on dates (completed section) Erm need to make it so it only shows dates

Implement search functionality

Get email credentials.
    - Get it implemented.

